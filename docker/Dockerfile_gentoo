# syntax=docker/dockerfile:1
# Gentoo is known for its flexibility and performance.
# This Dockerfile offers a balance between stability, security, and modernity.
FROM gentoo/stage3:latest@sha256:11c63ffc7dd21d1092c6a1ccce4459da03e459298593eedc7e2b25dd0f48c746
ENV TERM=xterm

# Define the default version of nodebuilder or pass in a different version (eg master) as an argument
ARG NODEBUILDER_VERSION=v1.4.0

# Define the nodebuilder URL
ARG NODEBUILDER_URL=https://github.com/bitcoin-tools/nodebuilder/raw/${NODEBUILDER_VERSION}/nodebuilder

# Define the dependencies URL
ARG DEPENDENCIES_URL=https://github.com/bitcoin-tools/nodebuilder/raw/841-feat-add-support-and-dockerfile-for-gentoo-linux/resources/dependencies/runtime_dependencies_emerge.txt

# Check the current environment
RUN uname -a \
  && cat /etc/os-release \
  && df -h \
  && grep Mem /proc/meminfo \
  && date -u

# Install sudo dependency
RUN mkdir -p /var/db/repos/gentoo \
  && emerge-webrsync --quiet \
  && emerge --jobs "$(nproc)" --load-average "$(($(nproc) + 1))" --quiet --quiet-build --quiet-fail app-admin/sudo

# Download and execute the script
RUN /bin/sh -x "$(curl -fsSL ${NODEBUILDER_URL} )"
